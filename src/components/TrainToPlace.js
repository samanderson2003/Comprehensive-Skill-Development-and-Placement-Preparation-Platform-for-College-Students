import React from "react";

const TrainToPlace = () => {
  return (
    <div style={styles.container}>
      <h1>🚀 Train to Place</h1>
      <p>Prepare with aptitude tests, coding challenges, and interview questions.</p>
    </div>
  );
};

const styles = {
  container: {
    fontFamily: "Arial, sans-serif",
    textAlign: "center",
    color: "#333",
    backgroundColor: "#f9f9f9",
    minHeight: "100vh",
    padding: "20px",
  },
};

export default TrainToPlace;

